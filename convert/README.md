# Tools for converting Markdown files into Jupyter Notebooks

To convert the markdown files in this tutorial into `.ipynb` files that are compatible with Jupyter or Google Colab, run:

```bash
./convert-myst.sh
```
